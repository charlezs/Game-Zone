<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="comment-title">
           <span class="name"> {{user.username}} </span> on {{format_date created_at}}
        </div>
        <div class="comment-text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>